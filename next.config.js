/** @type {import('next').NextConfig} */
// Dit bestand bevat instellingen voor Next.js zelf (het framework van je site).
const nextConfig = {
    reactStrictMode: true, // Helpt bij het vinden van fouten tijdens het programmeren
}

module.exports = nextConfig
